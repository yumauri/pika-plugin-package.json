{
  "name": "example",
  "description": "Example of use of the pika-plugin-package.json",
  "version": "1.0.0",
  "author": "Victor Didenko <yumaa.verdin@gmail.com> (https://yumaa.name)",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf pkg && yarn build --prefix ../ && pika build"
  },
  "devDependencies": {
    "@pika/pack": "^0.5.0",
    "@pika/plugin-standard-pkg": "^0.7.1"
  },
  "@pika/pack": {
    "pipeline": [
      [
        "@pika/plugin-standard-pkg"
      ],
      [
        "../pkg"
      ]
    ]
  }
}
